language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t zxcarrot/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true zxcarrot/docker-react npm run test -- --coverage
